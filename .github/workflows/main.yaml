name: Release application

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

env:
  PROJECT_ID: ${{ secrets.GKE_PROJECT }}
  GKE_CLUSTER: dwk-gke
  GKE_ZONE: europe-north1-b
  REGISTRY: europe-north1-docker.pkg.dev
  REPOSITORY: devops-with-kubernetes-image-hub
  APP_IMAGE: todo-app
  SERVER_IMAGE: todo-server
  APP_DEPLOYMENT: todo-app-dep
  SERVER_DEPLOYMENT: todo-app-server-dep

jobs:
  build-publish-deploy:
    name: Build, Publish and Deploy
    runs-on: ubuntu-latest
    environment: GKE_PROJECT

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: "${{ secrets.WORKLOAD_IDENTITY_PROVIDER }}"
          service_account: "${{ secrets.SERVICE_ACCOUNT }}"

      - name: 'Set up Cloud SDK'
        uses: google-github-actions/setup-gcloud@v3

      - name: 'Use gcloud CLI'
        run: gcloud info

      - run: gcloud --quiet auth configure-docker "$REGISTRY"

      - name: 'Get GKE credentials'
        uses: 'google-github-actions/get-gke-credentials@v3'
        with:
          cluster_name: '${{ env.GKE_CLUSTER }}'
          project_id: '${{ env.PROJECT_ID }}'
          location: '${{ env.GKE_ZONE }}'

      - name: Form image names
        run: |
          echo "APP_IMAGE_TAG=$REGISTRY/$PROJECT_ID/$REPOSITORY/$APP_IMAGE:$GITHUB_SHA" >> "$GITHUB_ENV"
          echo "SERVER_IMAGE_TAG=$REGISTRY/$PROJECT_ID/$REPOSITORY/$SERVER_IMAGE:$GITHUB_SHA" >> "$GITHUB_ENV"

      - name: Build app image
        working-directory: chapter-4/exercise-3.6/todo-app
        run: docker build --platform linux/amd64 --tag "$APP_IMAGE_TAG" .

      - name: Build server image
        working-directory: chapter-4/exercise-3.6/todo-server
        run: docker build --platform linux/amd64 --tag "$SERVER_IMAGE_TAG" .

      - name: Publish app image
        run: docker push "$APP_IMAGE_TAG"

      - name: Publish server image
        run: docker push "$SERVER_IMAGE_TAG"

      - name: Set up Kustomize
        uses: imranismail/setup-kustomize@v3

      - name: Deploy
        working-directory: chapter-4/exercise-3.6/manifests
        run: |-
          kubectl create namespace $GITHUB_REF_NAME || true
          kubectl config set-context --current --namespace=$GITHUB_REF_NAME
          kustomize edit set namespace $GITHUB_REF_NAME
          kustomize edit set image \
            APP/IMAGE="$APP_IMAGE_TAG" \
            SERVER/IMAGE="$SERVER_IMAGE_TAG"
          kustomize build . | kubectl apply -f -
          kubectl rollout status deployment "$APP_DEPLOYMENT" --timeout=5m
          kubectl rollout status deployment "$SERVER_DEPLOYMENT" --timeout=5m
          kubectl get services -o wide
