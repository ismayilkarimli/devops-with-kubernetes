apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: picture-storage-pvc
spec:
  storageClassName: picture-storage-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 128Mi