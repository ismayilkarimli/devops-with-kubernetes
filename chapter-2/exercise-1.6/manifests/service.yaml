apiVersion: v1
kind: Service
metadata:
  name: ping-app-svc
spec:
  type: NodePort
  selector:
    app: ping-app
  ports:
    - name: http
      nodePort: 30080
      protocol: TCP
      port: 1234
      targetPort: 8081
