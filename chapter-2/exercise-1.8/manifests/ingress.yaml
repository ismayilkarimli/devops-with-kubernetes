apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ping-app-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ping-app-svc
                port:
                  number: 2345
